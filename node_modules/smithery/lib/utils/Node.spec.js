"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
//setting up test environment
const chai_1 = require("chai");
require("mocha");
const Node_1 = require("./Node");
describe('Check the Utility Class Node, to prove it is correct working', () => {
    it('Check if Node is createable without any parameters', () => {
        chai_1.expect(new Node_1.Node()).not.to.be.undefined;
    });
    it('Check if Node creates with position parameter', () => {
        const n = new Node_1.Node(1);
        chai_1.expect(n).not.to.be.undefined;
        chai_1.expect(n.start).equal(1);
    });
    it('Check if Node can be correctly copied, without just copying the node reference', () => {
        const n = new Node_1.Node();
        n.type = 'test';
        n.name = 'Test-Name';
        n.path = 'root';
        n.sourePath = 'elsewhere';
        n.setAttribute('att1', 'value1');
        const copy_n = n.clone();
        //checks
        chai_1.expect(copy_n).not.equal(n);
        chai_1.expect(copy_n.type).equal(n.type);
        chai_1.expect(copy_n.path).equal(n.path);
        chai_1.expect(copy_n.sourcePath).equal(n.sourcePath);
        chai_1.expect(copy_n.getAttribute('att1')).equal(n.getAttribute('att1'));
    });
});
//# sourceMappingURL=Node.spec.js.map