version: 1
start:
  command: ["python", "servidor.py"]
  port: 6277

configSchema:
  type: object
  properties:
    modelo:
      type: string
      default: "claude-3"
      enum: ["claude-3", "gpt-4"]
